# Frontend

Not part of this scaffold. Point any client (CLI, Streamlit, React, chat UI) at the
FastAPI backend in `app/`. All endpoints are documented automatically at `/docs`
(Swagger UI) once the server is running.

A simple starting option for a learning team: a Streamlit app that calls these
endpoints — fast to build, no frontend framework required.
